package main;

public interface ElevatorListener 
{
	/*
	 void arrive(int elId, int floor);

	  // called when internal button is pushed
	  void pushInternal(int elId, int floor);

	  // called when external button is pushed
	  void pushExternal(int floor, boolean isUp);

	  // called once the doors have finished opening
	  void doorsFullyOpen(int elId);

	  void doorsFullyClosed(int elId);
	  */
	
}
